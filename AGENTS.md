# 简化代码
我们要开始简化当前工程, 我们最终要将当前工程弄到可读性非常高
只要保证 `./pi-test.sh` 可以运行, 能正常交流即可

其他的多余代码慢慢删除

如果删除的代码导致我们运行`./pi-test.sh`失败 我们可以依赖git命令回退代码
你优化完毕代码 不要直接git提交, 人类会手动测试成功后 人类自己会提交代码

有些代码可以直接写死, 例如main函数中 appMode通过各种方法得出appMode的值为"interactive", 经过走读代码后, 我们可以直接删除多余代码, 直接给他复制
```
let appMode = "interactive";
```

我们的目的是简化代码, 不保证代码最终能发布
我们目的是研究最核心逻辑  不保证业务多路可用性
我们只保证正常的启动 不保证sdk方式能用

我们相信这个项目 80%代码是为了 程序的可用性
实际只有20%代码是正在的核心逻辑
我们保留最终路径的可用性  其他异常代码什么的都可以被优化掉

# 手段
手段不限制, 你可以采用以下方式来优化
1. 直接删除代码
2. 删除完全用不上的代码文件
3. 合并类
4. 合并文件
5. 重写代码
6. 删除一些结构体 类
7. 直接写死一些变量, 必然为ture, 必然为某个字符串


# 代码注释
当用户指定某个函数需要写注释的时候
除了简单的复制以外  该函数内尽量每一行都写中文注释, 当然函数名肯定要写注释, 写清楚这个函数是干什么的

# 记忆
将你已经优化(删除代码)过的文件 写入 `youhua.md`
直接删除文件的 不需要记录到`youhua.md`
单次操作5-20个文件

