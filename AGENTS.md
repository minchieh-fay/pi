# 简化代码
我们要开始简化当前工程, 我们最终要将当前工程弄到可读性非常高
只要保证 `./pi-test.sh` 可以运行, 能正常交流即可

其他的多余代码慢慢删除

如果删除的代码导致我们运行`./pi-test.sh`失败 我们可以依赖git命令回退代码


# 代码注释
当用户指定某个函数需要写注释的时候
除了简单的复制以外  该函数内尽量每一行都写中文注释, 当然函数名肯定要写注释, 写清楚这个函数是干什么的