# 开发规范

## 对话风格

- 回答要简短精炼
- 在提交记录、issue、PR 评论或代码中不要使用表情符号
- 不要使用客套话或闲聊式填充文字（例如：用"Thanks @user"而不是"Thanks so much @user!"）
- 只使用技术性叙述，直接明了
- 使用简洁、清晰、简单的语言。在不可避免使用术语时，先对其进行定义
- 解释非平凡的设计和问题的方式：先描述问题，再给出具体示例或简短追踪，然后给出解决方案。说明为什么该解决方案是必要的，并与可选的复杂性区分开来
- 优先使用具体行为和小型示例，而非抽象总结、密集术语或未解释的变更列表
- 当用户提问时，先回答问题，然后再进行编辑或运行实现命令
- 回复用户反馈或分析时，明确说明你同意还是不同意，然后再说你做了什么修改

## 代码质量

- 在进行大范围变更之前、编辑你未完全检查的文件之前、或被要求调查或审计时，先完整阅读文件。不要仅凭搜索片段进行大范围变更
- 除非绝对必要，否则不使用 `any`
- 将只有一个调用点的单行辅助函数内联
- 检查 node_modules 中的外部 API 类型；不要猜测
- **禁止使用内联导入**（`await import()`、`import("pkg").Type`、动态类型导入）。仅限顶层导入
- 永远不要通过移除或降级代码来修复因过时代码产生的类型错误；请升级依赖
- 在根配置检查的代码中使用可擦除的 TypeScript 语法（Node strip-only 模式）（`packages/*/src`、`packages/*/test`、`packages/coding-agent/examples`）：不要使用参数属性、`enum`、`namespace`/`module`、`import =`、`export =` 或任何其他需要 JS 输出的构造。使用显式字段配合构造函数赋值
- 在移除看似有意的功能或代码之前，务必先询问
- 除非用户要求，否则不保留向后兼容性
- 永远不要硬编码 key 检查（例如 `matchesKey(keyData, "ctrl+x")`）。将它们添加到 `DEFAULT_EDITOR_KEYBINDINGS` 或 `DEFAULT_APP_KEYBINDINGS` 的默认值中，使其保持可配置
- 永远不要直接修改 `packages/ai/src/models.generated.ts`；应修改 `packages/ai/scripts/generate-models.ts`，然后重新生成。包含生成的 `models.generated.ts` 差异始终是可以的，即使重新生成包含了不相关的上游模型元数据变更

## 命令

- 代码变更之后（不是文档）：运行 `npm run check`（完整输出，不要截断）。在提交前修复所有错误、警告和提示信息。不会运行测试
- 除非用户要求，否则不要运行 `npm run build` 或 `npm test`
- 永远不要直接运行完整的 vitest 套件：它会包含在存在 endpoint/auth 环境变量时激活的 e2e 测试。对于所有非 e2e 测试，从仓库根目录运行 `./test.sh`。否则从包根目录运行特定测试：
  - Vitest: `node "$(git rev-parse --show-toplevel)/node_modules/vitest/dist/cli.js" --run test/specific.test.ts`
  - `packages/tui` (`node:test`): `node --test test/specific.test.ts`
- 如果你创建或修改了测试文件，请运行它并迭代测试或实现直至通过
- 对于 `packages/coding-agent/test/suite/`，使用 `test/suite/harness.ts` + faux provider。不要使用真实的 provider API、密钥或付费 token
- 当为修复 github issue 编写回归测试时，在测试旁添加包含 github issue 编号的注释
- 对于临时脚本，将它们 `write` 到临时文件（例如 `/tmp`），运行，必要时进行编辑，完成后删除。不要在 `bash` 命令中嵌入多行脚本
- 除非用户要求，否则不要提交

## 依赖与安装安全

- 将 npm 依赖和 lockfile 变更视为已审查的代码。直接外部依赖保持在精确版本
- 更新 `undici` 时，你**必须**阅读其目标版本的 changelog/发布说明，并评估任何变更是否可能影响功能，然后才能应用更新
- 本地 hydrate/更新使用 `npm install --ignore-scripts`；干净/CI 风格使用 `npm ci --ignore-scripts`。除非用户要求，否则不要运行生命周期脚本
- 如果 dep 元数据发生变化，使用 `npm install --package-lock-only --ignore-scripts` 刷新 `package-lock.json`
- 如果 `packages/coding-agent/npm-shrinkwrap.json` 需要重新生成，运行 `node scripts/generate-coding-agent-shrinkwrap.mjs`（使用 `--check` 或 `npm run check` 验证）。带有生命周期脚本的新依赖需要审查并在该脚本中添加显式白名单条目；不要静默添加
- 除非设置 `PI_ALLOW_LOCKFILE_CHANGE=1`，否则 pre-commit 会阻止 lockfile 提交。除非用户希望提交 lockfile 变更，否则不要绕过

## Git

多个 pi 会话可能同时在当前工作目录运行，每个会话修改不同的文件。触及你自己变更之外的未暂存、已暂存或未跟踪文件的 Git 操作会破坏其他会话的工作。请遵循以下规则：

提交：

- 只提交你在**本次会话**中修改的文件
- 显式指定路径进行暂存（`git add <path1> <path2>`）；永远不要使用 `git add -A` / `git add .`
- 提交前，运行 `git status` 并确认你只暂存了自己的文件
- `packages/ai/src/models.generated.ts` 可以始终随你的文件一起包含
- 消息格式：`{feat,fix,docs}[(ai,tui,agent,coding-agent)]: <commit message>（可选多行）`。消息应简洁且有信息量

永远不要运行（会破坏其他 agent 的工作或绕过检查）：

- `git reset --hard`、`git checkout .`、`git clean -fd`、`git stash`、`git add -A`、`git add .`、`git commit --no-verify`

如果 rebase 冲突发生：

- 仅在你修改的文件中解决冲突
- 如果冲突出现在你没有修改的文件中，中止操作并询问用户
- 永远不要 force push

## Issues 与 PRs

参见 `CONTRIBUTING.md` 了解贡献者门禁（自动关闭工作流、`lgtm`/`lgtmi`、质量要求）。

审阅 PR 时：

- 除非用户明确要求，否则不要运行 `gh pr checkout`、`git switch` 或将 worktree 移动到 PR 分支
- 使用 `gh pr view`、`gh pr diff`、`gh api` 以及针对拉取引用的本地 `git show`/`git diff` 来检查 PR 元数据、提交和补丁，而不要切换分支
- 如果需要 PR 文件内容，拉取/读取到临时文件或使用 `git show <ref>:<path>`，不要切换分支

创建 issue 时：

- 为受影响的包添加 `pkg:*` 标签（`pkg:agent`、`pkg:ai`、`pkg:coding-agent`、`pkg:tui`）；使用所有适用的标签

发布 issue/PR 评论时：

- 将评论写入临时文件，并使用 `gh issue/pr comment --body-file` 发布（不要通过 `--body` 发布多行 markdown）
- 保持评论简洁、技术性，符合用户的语气
- 在每个 AI 发布的评论末尾附上原始提示指定的 AI 生成免责声明行（例如 `This comment is AI-generated by `/wr``）

通过提交关闭 issue 时：

- 在消息中包含 `fixes #<number>` 或 `closes #<number>`，以便合并时自动关闭 issue。对于多个 issue，每个 issue 重复关键字（`closes #1, closes #2`）；共享关键字（`closes #1, #2`）仅关闭第一个

## 使用 tmux 测试 pi 交互模式

在受控终端中运行 TUI（从仓库根目录）：

```bash
tmux new-session -d -s pi-test -x 80 -y 24
tmux send-keys -t pi-test "./pi-test.sh" Enter
sleep 3 && tmux capture-pane -t pi-test -p     # 启动后捕获
tmux send-keys -t pi-test "your prompt here" Enter
tmux send-keys -t pi-test Escape               # 特殊按键（C-o 表示 ctrl+o 等）
tmux kill-session -t pi-test
```

## Changelog

位置：`packages/*/CHANGELOG.md`（每个包一个）。

`## [Unreleased]` 下的章节：`### Breaking Changes`（需要迁移的 API 变更）、`### Added`、`### Changed`、`### Fixed`、`### Removed`。

规则：

- 所有新条目都放在 `## [Unreleased]` 下。先阅读完整部分，然后追加到现有子节；切勿重复
- 已发布的版本部分（例如 `## [0.12.2]`）是不可变的；永远不要修改它们
- 在非 `main` 分支或 pull request 上工作时，不要创建 changelog 条目

署名：

- 内部（来自 issue）：`Fixed foo bar ([#123](https://github.com/earendil-works/pi-mono/issues/123))`
- 外部贡献：`Added feature X ([#456](https://github.com/earendil-works/pi-mono/pull/456) by [@username](https://github.com/username))`

## 发布

**锁步版本控制**：所有包共享一个版本；每次发布都会一起更新。`patch` = 修复 + 新增，`minor` = 破坏性变更。没有 major 发布。

1. **更新 CHANGELOG**：询问用户是否在 `main` 的最新提交上运行了 `/cl` 提示。如果没有，他们必须先运行 `/cl` 来审计并更新每个包的 `[Unreleased]` 部分，然后才能发布。

2. **本地冒烟测试**：在仓库外构建一个未发布的版本并进行冒烟测试（这样它无法解析 workspace 文件）：
   ```bash
   npm run release:local -- --out /tmp/pi-local-release --force
   cd /tmp

   # Node package 安装冒烟测试
   /tmp/pi-local-release/node/pi --help
   /tmp/pi-local-release/node/pi --version
   /tmp/pi-local-release/node/pi --list-models
   /tmp/pi-local-release/node/pi -p "Say exactly: ok"
   /tmp/pi-local-release/node/pi

   # Bun binary 冒烟测试
   /tmp/pi-local-release/bun/pi --help
   /tmp/pi-local-release/bun/pi --version
   /tmp/pi-local-release/bun/pi --list-models
   /tmp/pi-local-release/bun/pi -p "Say exactly: ok"
   /tmp/pi-local-release/bun/pi
   ```
   验证 Node 和 Bun 的启动、模型/账户列表、交互式启动，以及至少一次使用预期默认 provider 的真实 prompt。裸命令 `/tmp/pi-local-release/node/pi` 和 `/tmp/pi-local-release/bun/pi` 启动交互模式；在 tmux 中分别运行，提交一个 prompt，并等待模型回复后再认为交互式冒烟测试通过。除非用户明确接受风险，否则失败是发布阻塞项。

3. **运行发布脚本**：
   ```bash
   PI_ALLOW_LOCKFILE_CHANGE=1 npm_config_min_release_age=0 npm run release:patch    # 修复 + 新增
   PI_ALLOW_LOCKFILE_CHANGE=1 npm_config_min_release_age=0 npm run release:minor    # 破坏性变更
   ```
   仅对发布命令使用 `npm_config_min_release_age=0`。否则，当当前 workspace 包版本最近已发布时，仓库的正常 npm 年龄门控可能会阻止发布 lockfile 刷新。在推送前审查发布创建的任何 lockfile 或 shrinkwrap 差异。

   发布脚本会提升所有包版本、更新 changelog、重新生成发布产物、运行 `npm run check`、提交 `Release vX.Y.Z`、打标签 `vX.Y.Z`、添加新的 `## [Unreleased]` changelog 部分、提交 `Add [Unreleased] section for next cycle`，然后推送 `main` 和标签。标签推送后不要重新运行发布脚本。

4. **CI 验证并发布 npm 版本**：推送 `vX.Y.Z` 标签会触发 `.github/workflows/build-binaries.yml`。`publish-npm` job 使用 GitHub Actions OIDC 的 npm 可信发布，环境为 `npm-publish`；不需要本地 `npm publish`、`npm whoami`、OTP 或 WebAuthn 流程。发布后，`announce-pi-dev-release` 验证每个公开 workspace 包都在精确的发布版本解析，且其 npm tarball 可用，然后将验证后的发布标记写入 R2。`pi.dev/api/latest-version` 读取该标记；在该 job 成功之前，绝不允许从 npm 宣布发布。

5. **如果 CI 发布或宣告失败**：检查失败的 job。发布辅助是幂等的，会跳过已在 npm 上的包版本；宣告 job 会在更新 R2 标记之前重新检查可用性。修复 CI 或瞬态 npm 问题后重新运行失败的 job 或工作流。不要对同一版本重新运行 `npm run release:patch` 或 `npm run release:minor`。

## 用户覆盖

如果用户的指令与本文件中的任何规则冲突，在覆盖之前询问明确确认。只有在那之后才执行他们的指令。
